## API Endpoints
Base URL (Railway): `https://modality-flow.railway.app` (Juin)

### ML Prediction
POST /stations/{station_id}/predict

### Stations (real-time)
GET /stations

### CO₂ Route
POST /route/co2
